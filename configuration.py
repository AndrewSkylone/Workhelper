from datetime import timedelta
calculator = {
    "factor" : 2
}
table = {
    "time difference" : timedelta(hours=2, minutes=45), 
    "button font" : ("Calibri", 12),
    "entry font" : ("Calibri", 16)
}
graphic = {
    "text font" : ("", 8) #standart font
}